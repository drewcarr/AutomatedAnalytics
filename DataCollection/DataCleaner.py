# DataCleaner class for cleaning and transforming the data
class DataCleaner:
    def clean_data(self, raw_data: str):
        # Example cleaning process
        return raw_data.replace("Sample", "Cleaned")
