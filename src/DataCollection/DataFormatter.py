# DataFormatter class for formatting data into the final structure
class DataFormatter:
    def format_data(self, cleaned_data: str):
        # Example formatting process
        return {"formatted_data": cleaned_data}